name: validate
on:
  pull_request:
  push:
    branches: [main]
jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - run: pip install -r requirements.txt
      - name: Run tests
        run: python -m pytest tests/ -v
      - name: Validate models.yaml
        run: python scripts/validate.py
      - name: Ensure generated files are up to date
        run: |
          python scripts/render_readme.py
          python scripts/render_json.py
          git diff --exit-code README.md models.json \
            || (echo "README.md/models.json are stale — run scripts/render_readme.py and scripts/render_json.py, then commit." && exit 1)
